import React from 'react';import { Text, View, StyleSheet } from 'react-native';import { colors, radius, spacing } from '../theme';import type { CategoryKey } from '../types';export default function CategoryPill({ category }:{ category: CategoryKey }){if(!category){return null as any;}const color=(colors.categories as any)[category]||colors.accent;const label:Record<CategoryKey,string>={work:'РАБОТА',home:'ДОМ',global:'ГЛОБАЛЬНОЕ',habit:'ПОВТОР',personal:'ЛИЧНОЕ',urgent:'СРОЧНО'};return(<View style={[styles.pill,{backgroundColor:`${color}22`,borderColor:color}]}><Text style={[styles.text,{color}]}>{label[category]}</Text></View>);}const styles=StyleSheet.create({pill:{paddingVertical:4,paddingHorizontal:spacing(1.5),borderRadius:radius.lg,alignSelf:'flex-start',borderWidth:1},text:{fontSize:12,fontWeight:'700',letterSpacing:0.5}});